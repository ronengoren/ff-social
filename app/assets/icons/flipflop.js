import {createIconSetFromFontello} from 'react-native-vector-icons';
import fontelloConfig from '../fonts/flipflop/config.json';

const Icon = createIconSetFromFontello(fontelloConfig);
export default Icon;
